% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_tost}
\alias{plot_tost}
\title{Forest plot of GMR and 90 percent confidence intervals from TOST analysis}
\usage{
plot_tost(
  result,
  method = c("both", "normal", "gallant"),
  delta = log(1.25),
  col_inside = "#2D6A4F",
  col_outside = "#D62828",
  col_zone = "#B7E4C7"
)
}
\arguments{
\item{result}{A list returned by \code{\link[=run_MBTost]{run_MBTost()}}.}

\item{method}{Character. Which inference to display: \code{"both"} (default),
\code{"normal"}, or \code{"gallant"}.}

\item{delta}{Numeric. Equivalence margin on the log scale (default
\code{log(1.25)}); determines the acceptance zone drawn on the plot.}

\item{col_inside}{Character. Colour used when the CI lies entirely within
the acceptance zone (default dark green \code{"#2D6A4F"}).}

\item{col_outside}{Character. Colour used when the CI extends beyond a
boundary (default red \code{"#D62828"}).}

\item{col_zone}{Character. Fill colour of the acceptance zone rectangle
(default light green \code{"#B7E4C7"}).}
}
\value{
Invisibly returns the \code{data.frame} used for plotting, with columns
\code{metric}, \code{method}, \code{gmr}, \code{lower}, \code{upper}, and \code{be}.
}
\description{
Displays a horizontal forest plot of the geometric mean ratio (GMR) and its
90 \% confidence interval(s) for each available endpoint (AUC, Cmax).  The
bioequivalence acceptance zone [0.80, 1.25] is highlighted in green.
Normal and/or Gallant-corrected confidence intervals can be shown.
}
\examples{
\dontrun{
dp  <- system.file("extdata", "dataset_1.txt", package = "MBbioeq")
res <- run_MBTost(dp, model = "1cpt")
plot_tost(res)
plot_tost(res, method = "gallant")   # Gallant CIs only
}
}
\seealso{
\code{\link[=run_MBTost]{run_MBTost()}} to obtain the \code{result} argument;
\code{\link[=plot_bot]{plot_bot()}} for the BOT equivalent.
}
