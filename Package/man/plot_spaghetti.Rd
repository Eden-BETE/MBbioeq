% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_spaghetti}
\alias{plot_spaghetti}
\title{Spaghetti plot of individual PK concentration-time profiles}
\usage{
plot_spaghetti(
  data_path,
  title = "Individual PK Profiles",
  col_T = "#E63946",
  col_R = "#457B9D",
  add_mean = TRUE,
  alpha_ind = 0.35,
  xlab = "Time (hr)",
  ylab = "Concentration (mg/L)"
)
}
\arguments{
\item{data_path}{Character. Path to the dataset file (same format as
accepted by \code{\link[=run_MBTost]{run_MBTost()}} and \code{\link[=run_MBbot]{run_MBbot()}}; separator is detected
automatically).}

\item{title}{Character. Plot title.}

\item{col_T}{Character. Colour for Test (T) lines and mean profile
(default \code{"#E63946"}, red).}

\item{col_R}{Character. Colour for Reference (R) lines and mean profile
(default \code{"#457B9D"}, blue).}

\item{add_mean}{Logical. If \code{TRUE} (default), overlay the group mean
profile as a thicker line.}

\item{alpha_ind}{Numeric in (0, 1]. Transparency of individual lines
(\code{1} = fully opaque, default \code{0.35}).}

\item{xlab}{Character. X-axis label.}

\item{ylab}{Character. Y-axis label.}
}
\value{
Invisibly returns the pre-processed \code{data.frame} (output of
\code{\link[=prep_parallel_TR]{prep_parallel_TR()}}, NA concentrations removed).
}
\description{
Plots individual concentration-time profiles coloured by treatment group
(Test or Reference), with an optional overlay of the arithmetic mean
profile per group.  Useful for checking data quality, identifying outliers,
and visually comparing the two formulations before model fitting.
}
\examples{
\dontrun{
dp <- system.file("extdata", "dataset_1.txt", package = "MBbioeq")
plot_spaghetti(dp)
# Custom colours:
plot_spaghetti(dp, col_T = "darkorange", col_R = "steelblue")
}
}
\seealso{
\code{\link[=prep_parallel_TR]{prep_parallel_TR()}}, \code{\link[=run_MBTost]{run_MBTost()}}, \code{\link[=run_MBbot]{run_MBbot()}}
}
